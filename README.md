# dockerSea
A centralized multi-host Docker manager that can manage an unlimited number of VPS instances without installing any agent on the remote servers. It connects through SSH using SSH keys, allowing a single manager to control multiple Docker hosts, containers, images, volumes, networks, logs, and commands.
