di = """
src/
├── lib/
│   ├── domains/               # Regras e serviços por domínio (Containers, Images, Volumes, etc.)
│   │   ├── containers/
│   │   ├── images/
│   │   └── ...
│   └── shared/                # TUDO que é compartilhado
│       ├── components/        # Componentes UI reutilizáveis (buttons, modals, etc.)
│       ├── locales/           # pt-BR.json, en-US.json
│       ├── navigation/        # Controle de rotas e navegação
│       ├── session/           # Estado da sessão ativa (VPS, Profile)
│       ├── stores/            # Stores globais (locale, notifications, refresh, SWR)
│       └── events.ts          # Tópicos de eventos globais
├── pages/
├── App.svelte
└── main.ts
"""
